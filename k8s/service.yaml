apiVersion: v1
kind: Service
metadata:
  name: forecast-service
  namespace: ai-model-serving
  labels:
    app: forecast-service
spec:
  type: ClusterIP
  selector:
    app: forecast-service
  ports:
    - name: http
      port: 80
      targetPort: http
