global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts/rules.yml

scrape_configs:
  - job_name: forecast-service
    static_configs:
      - targets: ["forecast-service:8000"]
    metrics_path: /metrics
